do "pip install -r requirements.txt" to download packages in our requirement.txt

manual installations would be:
step 1:
cd to secure_sole
step 2:
pip install django
pip install django-otp
pip install django-allauth
pip install PyJWT
pip install django_ratelimit
pip install scikit-learn
pip install requests
step 3:
If manual authentication using google authenticator app does not work, downgrade to python version 3.11.8 then close the vs studio and open it and try registering a new user again. 
upon trying to solve this error we come up with a solution that a user should have the same version as the developers. thus, it is required for users to follow the same python version.
step 4:
for trying to access admin account to track the recorded actions of users
username would be: "admin111"
password would be: "admin111"
